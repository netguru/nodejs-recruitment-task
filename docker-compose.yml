version: "3.8"

services:
  nginx:
    build: ./nginx
    links:
      - auth
      - movies
    ports:
      - 8888:80

  auth:
    build: ./auth

  movies:
    build: ./movies
    links:
      - mongo

  mongo:
    image: mongo:latest
    volumes:
      - mongo-db:/data/db

volumes:
  mongo-db: