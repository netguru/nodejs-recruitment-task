name: movie-svc

on:
  push:
    paths:
      - 'movie/**'

  pull_request:
    paths:
      - 'movie/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd movie && npm install && npm run test
