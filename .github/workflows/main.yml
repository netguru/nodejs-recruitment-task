name: Main
on:
  push:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JWT_SECRET: secret
      MONGO_USERNAME: dbnurudeen
      MONGO_PASSWORD: nurudeen992
      MONGO_HOST: cluster0.89qyi.mongodb.net/recruitment

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2
        with: 
          node-version: "14.x"
      - name: Install dependencies
        run: npm install
      - name: Run test
        run: npm test
