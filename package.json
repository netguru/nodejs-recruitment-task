{
  "name": "nodejs-recruitment-task",
  "scripts": {
    "prepare": "husky install",
    "lint:authentication": "eslint 'services/authentication/{src,test}/**/*.ts'",
    "lint:movies": "eslint 'services/movies/{src,test}/**/*.ts'",
    "lint:shared": "eslint 'shared/src/**/*.ts'",
    "lint": "eslint 'services/*/{src,test}/**/*.ts'",
    "format:authentication": "eslint 'services/authentication/{src,test}/**/*.ts' --fix",
    "format:movies": "eslint 'services/movies/{src,test}/**/*.ts' --fix",
    "format:shared": "eslint 'shared/src/**/*.ts' --fix",
    "format": "eslint 'services/*/{src,test}/**/*.ts' --fix",
    "test:authentication": "jest 'services/authentication'",
    "test:movies": "jest 'services/movies'",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "typeorm": "^0.2.36"
  },
  "devDependencies": {
    "@types/supertest": "^2.0.11",
    "eslint-config-airbnb-base-ts": "^14.1.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "husky": "^7.0.1",
    "supertest": "^6.1.4",
    "ts-jest": "^27.0.4"
  }
}
