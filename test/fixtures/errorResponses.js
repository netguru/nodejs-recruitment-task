const errorResponses = {
  invalidPayload: { error: "invalid payload" },
  internalError: { error: "internal server error" },
  invalidCredentials: { error: "invalid username or password" },
};

module.exports = errorResponses;
